# Database Configuration
DATABASE_URL=

# Anthropic API
ANTHROPIC_API_KEY=

# App Settings
HOST=0.0.0.0
PORT=8000
DEBUG=False

# Security
HR_PASSWORD=159753
JWT_SECRET_KEY=halyk-hr-forum-super-secret-key-change-in-production

# LDAP/Active Directory Authentication
# Set LDAP_ENABLED=True to activate LDAP authentication
# When False, any password will be accepted for whitelisted users (TESTING ONLY!)
LDAP_ENABLED=False

# LDAP Server Configuration
# Replace these placeholders with your actual Active Directory configuration
LDAP_DOMAIN=PLACEHOLDER_DOMAIN
LDAP_HOST=placeholder-ldap-server.local
LDAP_PORT=389
LDAP_BASE_DN=OU=PLACEHOLDER,DC=PLACEHOLDER,DC=local
LDAP_USE_SSL=False
LDAP_USE_TLS=False
LDAP_TIMEOUT=10

# Permitted Users (Whitelist)
# Format: USER_ID:NAME:ROLE:PERMISSIONS
# Multiple users separated by semicolons
# Example: 00058215:Nadir:admin:read,write,admin;00037099:Saltanat:admin:read,write,admin;00060673:Meirim:manager:read,write
# Leave empty to use default placeholder users (PLACEHOLDER_ID_1, PLACEHOLDER_ID_2, PLACEHOLDER_ID_3)
PERMITTED_USERS=

# reCAPTCHA
RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=

# Test Configuration
TEST_TIME_LIMIT_MINUTES=40

# Weighted Score Formula Weights
# These weights should sum to 1.0 for proper calculation
# Formula: weighted_score = ((test_score * TEST_WEIGHT) + (mgr_rating * MANAGER_WEIGHT) + (self_rtg * SELF_WEIGHT)) / (max_score + 10 + 10) * 100
TEST_WEIGHT=0.5
MANAGER_WEIGHT=0.4
SELF_WEIGHT=0.1
